spring.application.name=Eureka-service
server.port=8761
eureka.instance.hostname=localhost

# Disable client registration and fetching registry since this is the server
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

# FreeMarker configuration
spring.freemarker.cache=true

# Correct the URL for the default zone
eureka.client.serviceUrl.defaultZone=http://52.87.190.213:8761

# Self-preservation configuration
eureka.server.enableSelfPreservation=false
management.endpoints.web.exposure.include=*
